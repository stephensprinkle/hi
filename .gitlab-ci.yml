image: node:latest

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

before_script:
  - apt-get update && apt-get upgrade -y
  - apt-get install git-core rsync -y

build-app:
  stage: build
  only:
    - master
  script:
    - npm i
    - npm run build

deploy-to-github:
  stage: deploy
  only:
    - master
  script:
    - git config --global user.email "${GIT_EMAIL}"
    - git config --global user.name "${GIT_NAME}"
    - git remote add github https://${GITHUB_USER}:${GITHUB_PAT}@github.com/stephensprinkle/hi.git
    - npm i
    - npm run build
    - cp CNAME ./docs/
    - git add .
    - git commit -m "Automated deploy commit based on -- $CI_COMMIT_MESSAGE | $CI_COMMIT_SHA"
    - git push -u github master 
